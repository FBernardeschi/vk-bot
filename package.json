{
  "scripts": {
    "build": "tsc --build tsconfig.json",
    "start": "node dist/bot.js",
    "deploy": "git pull && npm i && npm run build && npm run start && systemctl restart vk-bot"
  },
  "dependencies": {
    "dotenv": "^16.5.0",
    "vk-io": "^4.10.0"
  },
  "devDependencies": {
    "@types/node": "^22.15.17"
  }
}
